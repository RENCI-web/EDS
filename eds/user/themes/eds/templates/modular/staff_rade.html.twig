<?php  ?>
{% for post in taxonomy.findTaxonomy({'tag':'staff_rade'}) %}
    {% if taxonomy.findTaxonomy({'tag':'staff_rade'}) is empty %}
        <div></div>
    {% else %}
		<h1 class="mod-title-pub">Staff</h1>
		<hr class="mod-line-pub" />
	{% endif %}
{% endfor %}
<div id="featured-staff-section">
	<div class="featured-staff-wrap">
		{% for post in taxonomy.findTaxonomy({'tag':'staff_rade'}) %}
		    <div class="featured-staff"><a href="{{ post.url }}">
		    	<div class="featured-image-wrap">
				{% for image in post.media.images %}
				  {{ image.html }}
				{% endfor %}
				</div>
				<h3>{{ post.title }}</h3>
			</a></div>
		{% endfor %}
	</div>
</div>

